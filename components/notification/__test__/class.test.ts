import {Notify} from '../index'

describe('sp-notify :: classname', () => {
    it('basename', () => {
        // 通过
    //    let el = Notify({type:'info',title: '提示', message: '可以自动关闭',classname:'asd'});
    //     expect(el.className.indexOf > -1).toBeTruthy()
    })
})

// https://github.com/cofess/hexo-theme-pure.git